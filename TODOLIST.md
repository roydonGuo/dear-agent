[ ] dear-agent 的后端 rag 模块采用 modular 模块化设计：查询转换模块、路由模块、检索模块、重排序模块、上下文构建模块、生成模块、后处理模块。

[ ] agent 语音加上语气、语速控制。

[ ] dear-agent 前端的模型要能展示动作和表情，并随 agent 输出展示对应的动作或者表情。

[ ] dear-agent 后端增加上下文压缩功能。

[X] dear-agent 前端对话框语音输入。

[ ] dear-agent 对话入口选择模式：深度思考、deepresearch、planexe、图片生成等每种对应一种智能体。支持选择知识库进行 RAG， 参考来源功能就是引用溯源，让用户知道AI的回答是基于哪些信息来源的。来源记录，前端展示。

[ ] dear-agent 模型支持设置默认模型，后端模型模块上缓存。禁用的模型不可设置为默认模型。支持在对话框手动选择模型。

[ ] 新对话的前端对话列表刷新逻辑优化，优先用虚拟线程使用 ai 生成对话标题，并立即响应给前端（前端对话开始立即创建对话卡片，待后端返回标题和 id 后更新即可）。

[ ] 后端 skill 模块完善，前端展示具体技能列表。

[ ] TODO 记忆模块。

[ ] TODO 创作模块：图片生成、视频生成、文本转语音等。

[ ] live2d 区域添加背景。

[ ] 接入 IM 频道：优先接入飞书。

[ ] 优化sse，采用虚拟线程总结新对话title，响应给前端，前端手机语音并播放，用单独的对话卡片下方语音图标按钮控制开启/关闭语音播放（避免文字输出完了，但输入框一直处于禁用中状态）。
